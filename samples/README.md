# samples/

Put your local test audio here. **Audio files in this directory are git-ignored** so
you don't accidentally commit copyrighted learning tracks.

For automated tests we generate *synthetic* part-left files at runtime (see
`crates/audio-core/tests`), so nothing copyrighted is ever needed or stored.
